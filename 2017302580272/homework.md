## 分布式计算与网络第十次作业


### 习题

#### p15
```
a.不会，因为E在知道F的IP地址的情况下，能够知道它和F是在同一局域网中，故不会再传到R1，源和目标的IP和MAC地址分别是对方。
b.不会，只有在同一个局域网才会进行ARP查询。源IP为E的IP，目的MAC地址为R1连接E的接口的MAC地址
c.R1会向它的两个接口发送查询ARP包，路由器R1也会接受到，但是不会再继续传播，主机B接收到报文后不会再发送查询报文，因为从消息中便可知A的MAC地址，接收到B的响应会记录B的MAC地址
```


#### p17
```
对于10Mbps传输51200b需要5.12ms

对于100Mbps则需要0.512ms


```
#### p26
```
1. B发送一帧给E，由于交换机表是空的，所以交换机不知道与E的MAC地址对应的接口，学习了B并发送A,C,D,E,F。
2. E发送一帧给B，由于交换机已经知道与B的MAC地址对应的接口，直接发给B，并记录E。
3. A发送一帧给B	，由于交换机已经知道与B的MAC地址对应的接口。直接发给B。并记录A。
4. B发送一帧给A，由于交换机知道A,B两个接口，则直接发给A，且不发生改变。

```
